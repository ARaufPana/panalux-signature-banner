# Fully pinned dependency closure (direct + transitive), per security
# remediation R1 (July 2026). Policy: no floating versions — every package
# that installs in CI is pinned here, so no new PyPI release can enter the
# build without a reviewed commit. Dependabot (.github/dependabot.yml)
# opens weekly PRs to keep these current.
#
# Direct dependencies (4):
Pillow==11.3.0
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.1.0
# Transitive dependencies (6):
certifi==2026.6.17
charset-normalizer==3.4.7
idna==3.18
soupsieve==2.8.4
typing_extensions==4.16.0
urllib3==2.6.3
# Note: Flask + APScheduler removed July 2026 — they served only the
# never-deployed local dev server (app.py / banner/scheduler.py); the
# production path (regenerate.py) does not import them.
